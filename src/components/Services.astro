---
import { SERVICIOS_INTRO, services } from '../data/copy.ts';
import { langOf, t, servicePath } from '../data/i18n.ts';

const lang = langOf(Astro.currentLocale);
---

<section id="servicios" class="section">
  <div class="head" data-reveal>
    <div>
      <div class="head__label">{t(SERVICIOS_INTRO.label, lang)}</div>
      <h2 class="head__h2">{t(SERVICIOS_INTRO.h2, lang)}</h2>
    </div>
    <p class="head__para">{t(SERVICIOS_INTRO.para, lang)}</p>
  </div>

  <div class="table">
    {
      services.map((s) => (
        <a href={servicePath(s.slug, lang)} class="row" data-reveal>
          <span class="row__num">{s.num}</span>
          <h3 class="row__title">{t(s.title, lang)}</h3>
          <p class="row__desc">{t(s.desc, lang)}</p>
          <span class="row__arrow" aria-hidden="true">
            →
          </span>
        </a>
      ))
    }
  </div>
</section>

<style>
  .section {
    border-bottom: var(--line-primary);
  }

  /* --- cabecera de sección (compartida visualmente entre secciones) --- */
  .head {
    display: flex;
    align-items: flex-end;
    justify-content: space-between;
    padding: 54px var(--gutter) 40px;
    gap: 30px;
    flex-wrap: wrap;
  }

  .head__label {
    font-family: var(--font-mono);
    font-size: 12px;
    letter-spacing: 0.1em;
    color: var(--blue);
    margin-bottom: 18px;
  }

  .head__h2 {
    font-family: var(--font-display);
    font-weight: 900;
    font-size: clamp(40px, 6vw, 78px);
    line-height: 0.9;
    letter-spacing: -0.04em;
    text-transform: uppercase;
  }

  .head__para {
    font-family: var(--font-body);
    font-size: 16px;
    line-height: 1.5;
    max-width: 300px;
    color: var(--grey-body);
  }

  /* --- tabla de servicios --- */
  .table {
    border-top: var(--line-primary);
  }

  .row {
    display: grid;
    grid-template-columns: 90px 1.1fr 1.5fr 60px;
    align-items: center;
    gap: 24px;
    padding: 26px var(--gutter);
    border-bottom: 1px solid var(--hairline);
    color: var(--ink);
    text-decoration: none;
    transition: background 0.15s, color 0.15s;
  }

  /* Interacción firma: la fila entera invierte a azul + texto blanco. */
  .row:hover,
  .row:focus-visible {
    background: var(--blue);
    color: #fff;
  }

  .row__num {
    font-family: var(--font-mono);
    font-size: 14px;
    color: inherit;
    opacity: 0.6;
  }

  .row__title {
    font-family: var(--font-display);
    font-weight: 800;
    font-size: clamp(22px, 2.4vw, 34px);
    letter-spacing: -0.02em;
    text-transform: uppercase;
    color: inherit;
  }

  .row__desc {
    font-family: var(--font-body);
    font-size: 16px;
    line-height: 1.45;
    color: inherit;
  }

  .row__arrow {
    font-family: var(--font-mono);
    font-size: 22px;
    text-align: right;
    color: inherit;
  }

  /* --- responsive --- */
  @media (max-width: 900px) {
    .row {
      grid-template-columns: 60px 1fr 40px;
      gap: 16px 20px;
    }
    /* la descripción baja a una segunda fila bajo el título */
    .row__desc {
      grid-column: 2 / 3;
    }
  }

  @media (max-width: 600px) {
    .head {
      padding: 40px 20px 28px;
    }
    .row {
      grid-template-columns: 44px 1fr;
      gap: 8px 14px;
      padding: 22px 20px;
    }
    .row__desc {
      grid-column: 2 / 3;
    }
    .row__arrow {
      display: none;
    }
  }
</style>
