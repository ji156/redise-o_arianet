---
// Portada en euskera. Mismo layout que la castellana: el idioma sale del
// prefijo /eu/ de la ruta, que es lo que lee `Astro.currentLocale`.
import Home from '../../layouts/Home.astro';
---

<Home />
